import React from 'react';
import Header from '../Header/Header';
import Navbar from '../Navbar/Navbar';

const Home = () => {
    return (
        <div>
        <section id="home">             
            <Navbar></Navbar>
            <Header></Header>
        </section>
        </div>
    );
};

export default Home; 

